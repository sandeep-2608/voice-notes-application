{
  "name": "voice-notes-application",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "cd client && npm ci && npm run build",
    "install-server": "cd server && npm ci",
    "install-client": "cd client && npm ci"
  }
}
